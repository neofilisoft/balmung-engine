# Experiments

Prototype and legacy files that are not part of the supported editor/runtime build.

Notes:
- Keep production code in `src/`, `include/`, `editor/`, `bridge/`.
- Move incomplete rendering/editor experiments here to avoid confusing the main project tree.




